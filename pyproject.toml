[project]
name = "sqlens"
version = "0.1.0"
description = "Terminal UI SQLite database explorer"
requires-python = ">=3.10"
dependencies = [
    "textual>=8.2.7",
]

[project.scripts]
sqlens = "sqlens:main"
